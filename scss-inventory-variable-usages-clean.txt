assets/css/article-content.scss
  Type : article-css
  Style scoped : non
  Déclarations :
    - aucune
  Usages :
    - $space-s — assets/css/article-content.scss:8 — margin-bottom: $space-s;
    - $space-s — assets/css/article-content.scss:14 — margin-bottom: $space-s;
    - $space-2xs — assets/css/article-content.scss:18 — margin-bottom: $space-2xs;
    - $bold — assets/css/article-content.scss:23 — font-weight: $bold;
    - $gris3 — assets/css/article-content.scss:39 — background: linear-gradient($gris3, $gris3) right bottom / 100% 0.15em no-repeat;
    - $gris3 — assets/css/article-content.scss:39 — background: linear-gradient($gris3, $gris3) right bottom / 100% 0.15em no-repeat;
    - $vert — assets/css/article-content.scss:48 — background: linear-gradient($vert, $vert) right bottom / 100% 0.25em no-repeat;
    - $vert — assets/css/article-content.scss:48 — background: linear-gradient($vert, $vert) right bottom / 100% 0.25em no-repeat;
    - $space-xl — assets/css/article-content.scss:59 — margin-top: $space-xl;      // 64px -> 104px
    - $space-3xs — assets/css/article-content.scss:60 — margin-bottom: $space-3xs;  // 4px -> 6px
    - $space-l — assets/css/article-content.scss:75 — margin-top: $space-l;       // 40px -> 64px
    - $space-3xs — assets/css/article-content.scss:76 — margin-bottom: $space-3xs;  // 4px -> 6px
    - $space-m — assets/css/article-content.scss:90 — margin-top: $space-m;       // 24px -> 40px
    - $space-3xs — assets/css/article-content.scss:91 — margin-bottom: $space-3xs;  // 4px -> 6px
    - $gris4 — assets/css/article-content.scss:105 — border-left: 3px solid $gris4;
    - $gris2 — assets/css/article-content.scss:106 — color: $gris2;
    - $gris6 — assets/css/article-content.scss:115 — background: $gris6;
    - $gris1 — assets/css/article-content.scss:116 — color: $gris1;
    - $vert — assets/css/article-content.scss:131 — color: $vert;
    - $gris1 — assets/css/article-content.scss:147 — color: $gris1;
    - $gris6 — assets/css/article-content.scss:153 — background: $gris6;
    - $gris1 — assets/css/article-content.scss:154 — color: $gris1;
    - $normal — assets/css/article-content.scss:159 — font-weight: $normal;
    - $gris3 — assets/css/article-content.scss:223 — color: $gris3;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

assets/css/main.scss
  Type : global-css
  Style scoped : non
  Déclarations :
    - aucune
  Usages :
    - $fondGris — assets/css/main.scss:6 — background: $fondGris;
    - $fondClair — assets/css/main.scss:10 — background: $fondClair;
    - $gris1 — assets/css/main.scss:15 — color: $gris1;
    - $gris2 — assets/css/main.scss:19 — color: $gris2;
    - $gris3 — assets/css/main.scss:23 — color: $gris3;
    - $gris4 — assets/css/main.scss:27 — color: $gris4;
    - $gris5 — assets/css/main.scss:31 — color: $gris5;
    - $gris6 — assets/css/main.scss:35 — color: $gris6;
    - $bleu1 — assets/css/main.scss:39 — color: $bleu1;
    - $bleu2 — assets/css/main.scss:43 — color: $bleu2;
    - $vert — assets/css/main.scss:47 — color: $vert;
    - $light — assets/css/main.scss:55 — font-weight: $light;
    - $normal — assets/css/main.scss:59 — font-weight: $normal;
    - $bold — assets/css/main.scss:63 — font-weight: $bold;
    - $black — assets/css/main.scss:67 — font-weight: $black;
    - $vert — assets/css/main.scss:71 — border-left: 2px solid $vert;
    - $level-size — assets/css/main.scss:79 — font-size: calc($level-size*0.1068);
    - $level-size — assets/css/main.scss:83 — font-size: calc($level-size*0.125);
    - $type-base-size — assets/css/main.scss:130 — font-size: $type-base-size;
    - $type-base-size — assets/css/main.scss:134 — font-size: $type-base-size * 1.28;
    - $normal — assets/css/main.scss:141 — font-weight: $normal;
    - $fondClair — assets/css/main.scss:147 — background-color: $fondClair;
    - $bold — assets/css/main.scss:219 — font-weight: $bold;
    - $gris2 — assets/css/main.scss:221 — color: $gris2;
    - $gris4 — assets/css/main.scss:225 — color: $gris4;
    - $black — assets/css/main.scss:226 — font-weight: $black;
    - $bold — assets/css/main.scss:230 — font-weight: $bold;
    - $bleu2 — assets/css/main.scss:239 — background: $bleu2;
    - $gris1 — assets/css/main.scss:242 — box-shadow: 1px 1.5px 5px $gris1;
    - $vert — assets/css/main.scss:255 — background: $vert;
    - $vert — assets/css/main.scss:276 — color: $vert;
  Variables dans commentaires :
    - $gris1 — assets/css/main.scss:243 — // box-shadow: 2px 5px 10px 2px $gris1;
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — assets/css/main.scss:186 — @media (min-width: $breakpoint-tablet) {

assets/css/mixins/_mixins.scss
  Type : global-mixins
  Style scoped : non
  Déclarations :
    - aucune
  Usages :
    - aucun
  Variables dans commentaires :
    - $breakpoint-tablet — assets/css/mixins/_mixins.scss:10 — // $breakpoint-tablet moved to _typo.scss to avoid duplication with modern Sass @use
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

assets/css/vars/_colors.scss
  Type : global-vars
  Style scoped : non
  Déclarations :
    - $jaune — assets/css/vars/_colors.scss:1 — $jaune : #f2a81d;
    - $vert — assets/css/vars/_colors.scss:2 — $vert: #00a83e; // WCAG AA compliant (was #04d94f, ratio 2.4:1 → now 4.5:1)
    - $bleu1 — assets/css/vars/_colors.scss:3 — $bleu1: #2561d9;
    - $bleu2 — assets/css/vars/_colors.scss:4 — $bleu2: #0439d9;
    - $gris1 — assets/css/vars/_colors.scss:6 — $gris1: #0D0D0D;
    - $gris2 — assets/css/vars/_colors.scss:7 — $gris2: #404040;
    - $gris3 — assets/css/vars/_colors.scss:8 — $gris3: #737272;
    - $gris4 — assets/css/vars/_colors.scss:9 — $gris4: #8B8A89; // WCAG AA compliant (was #A6A5A4, ratio 2.9:1 → now 4.6:1)
    - $gris5 — assets/css/vars/_colors.scss:10 — $gris5: #F2F0F0;
    - $gris6 — assets/css/vars/_colors.scss:11 — $gris6: #d9d9d9;
    - $fondGris — assets/css/vars/_colors.scss:18 — $fondGris: #4a4a4a;
    - $fondClair — assets/css/vars/_colors.scss:19 — $fondClair: #f2f2f2;
    - $deg1 — assets/css/vars/_colors.scss:21 — $deg1: linear-gradient($vert 20.76%, $bleu1 89.73%);
    - $deg2 — assets/css/vars/_colors.scss:22 — $deg2: linear-gradient($bleu2 30%, $bleu1 80%);
    - $deg3 — assets/css/vars/_colors.scss:23 — $deg3: linear-gradient($jaune 23.78%, #f6b721 49.3%, #ffd329 100%);
    - $deg4 — assets/css/vars/_colors.scss:24 — $deg4: linear-gradient($vert 44.84%, #18ea62 68%, #30ff79 90.89%);
  Usages :
    - $vert — assets/css/vars/_colors.scss:21 — $deg1: linear-gradient($vert 20.76%, $bleu1 89.73%);
    - $bleu1 — assets/css/vars/_colors.scss:21 — $deg1: linear-gradient($vert 20.76%, $bleu1 89.73%);
    - $bleu2 — assets/css/vars/_colors.scss:22 — $deg2: linear-gradient($bleu2 30%, $bleu1 80%);
    - $bleu1 — assets/css/vars/_colors.scss:22 — $deg2: linear-gradient($bleu2 30%, $bleu1 80%);
    - $jaune — assets/css/vars/_colors.scss:23 — $deg3: linear-gradient($jaune 23.78%, #f6b721 49.3%, #ffd329 100%);
    - $vert — assets/css/vars/_colors.scss:24 — $deg4: linear-gradient($vert 44.84%, #18ea62 68%, #30ff79 90.89%);
  Variables dans commentaires :
    - $gris1 — assets/css/vars/_colors.scss:12 — // $gris1: #262626;
    - $gris2 — assets/css/vars/_colors.scss:13 — // $gris2: #595959;
    - $gris3 — assets/css/vars/_colors.scss:14 — // $gris3: #a6a2a1;
    - $gris4 — assets/css/vars/_colors.scss:15 — // $gris4: #d9d9d9;
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

assets/css/vars/_spacing.scss
  Type : global-vars
  Style scoped : non
  Déclarations :
    - $space-base — assets/css/vars/_spacing.scss:8 — $space-base: 1rem; // 16px
    - $space-ratio — assets/css/vars/_spacing.scss:11 — $space-ratio: 1.618;
    - $space-min-width — assets/css/vars/_spacing.scss:14 — $space-min-width: 320px;
    - $space-max-width — assets/css/vars/_spacing.scss:15 — $space-max-width: 1240px;
    - $min-width-rem — assets/css/vars/_spacing.scss:21 — $min-width-rem: math.div($space-min-width, 16px);
    - $max-width-rem — assets/css/vars/_spacing.scss:22 — $max-width-rem: math.div($space-max-width, 16px);
    - $slope — assets/css/vars/_spacing.scss:25 — $slope: math.div(($max-size - $min-size), ($max-width-rem - $min-width-rem));
    - $y-intercept — assets/css/vars/_spacing.scss:28 — $y-intercept: $min-size - ($slope * $min-width-rem);
    - $space-3xs — assets/css/vars/_spacing.scss:38 — $space-3xs: fluid-space(0.25, 0.375);    // 4px -> 6px
    - $space-2xs — assets/css/vars/_spacing.scss:41 — $space-2xs: fluid-space(0.5, 0.625);     // 8px -> 10px
    - $space-xs — assets/css/vars/_spacing.scss:44 — $space-xs: fluid-space(0.75, 1);         // 12px -> 16px
    - $space-s — assets/css/vars/_spacing.scss:47 — $space-s: fluid-space(1, 1.5);           // 16px -> 24px
    - $space-m — assets/css/vars/_spacing.scss:50 — $space-m: fluid-space(1.5, 2.5);         // 24px -> 40px
    - $space-l — assets/css/vars/_spacing.scss:53 — $space-l: fluid-space(2.5, 4);           // 40px -> 64px
    - $space-xl — assets/css/vars/_spacing.scss:56 — $space-xl: fluid-space(4, 6.5);          // 64px -> 104px
    - $space-2xl — assets/css/vars/_spacing.scss:59 — $space-2xl: fluid-space(6.5, 10.5);      // 104px -> 168px
    - $space-3xl — assets/css/vars/_spacing.scss:62 — $space-3xl: fluid-space(10.5, 17);       // 168px -> 272px
    - $space-3xs-2xs — assets/css/vars/_spacing.scss:66 — $space-3xs-2xs: fluid-space(0.25, 0.625);   // 4px -> 10px
    - $space-2xs-xs — assets/css/vars/_spacing.scss:67 — $space-2xs-xs: fluid-space(0.5, 1);         // 8px -> 16px
    - $space-xs-s — assets/css/vars/_spacing.scss:68 — $space-xs-s: fluid-space(0.75, 1.5);        // 12px -> 24px
    - $space-s-m — assets/css/vars/_spacing.scss:69 — $space-s-m: fluid-space(1, 2.5);            // 16px -> 40px
    - $space-m-l — assets/css/vars/_spacing.scss:70 — $space-m-l: fluid-space(1.5, 4);            // 24px -> 64px
    - $space-l-xl — assets/css/vars/_spacing.scss:71 — $space-l-xl: fluid-space(2.5, 6.5);         // 40px -> 104px
    - $space-xl-2xl — assets/css/vars/_spacing.scss:72 — $space-xl-2xl: fluid-space(4, 10.5);        // 64px -> 168px
    - $space-2xl-3xl — assets/css/vars/_spacing.scss:73 — $space-2xl-3xl: fluid-space(6.5, 17);       // 104px -> 272px
  Usages :
    - $space-min-width — assets/css/vars/_spacing.scss:21 — $min-width-rem: math.div($space-min-width, 16px);
    - $space-max-width — assets/css/vars/_spacing.scss:22 — $max-width-rem: math.div($space-max-width, 16px);
    - $max-size — assets/css/vars/_spacing.scss:25 — $slope: math.div(($max-size - $min-size), ($max-width-rem - $min-width-rem));
    - $min-size — assets/css/vars/_spacing.scss:25 — $slope: math.div(($max-size - $min-size), ($max-width-rem - $min-width-rem));
    - $max-width-rem — assets/css/vars/_spacing.scss:25 — $slope: math.div(($max-size - $min-size), ($max-width-rem - $min-width-rem));
    - $min-width-rem — assets/css/vars/_spacing.scss:25 — $slope: math.div(($max-size - $min-size), ($max-width-rem - $min-width-rem));
    - $min-size — assets/css/vars/_spacing.scss:28 — $y-intercept: $min-size - ($slope * $min-width-rem);
    - $slope — assets/css/vars/_spacing.scss:28 — $y-intercept: $min-size - ($slope * $min-width-rem);
    - $min-width-rem — assets/css/vars/_spacing.scss:28 — $y-intercept: $min-size - ($slope * $min-width-rem);
    - $space-3xs — assets/css/vars/_spacing.scss:77 — .space-3xs { margin-bottom: $space-3xs; }
    - $space-2xs — assets/css/vars/_spacing.scss:78 — .space-2xs { margin-bottom: $space-2xs; }
    - $space-xs — assets/css/vars/_spacing.scss:79 — .space-xs { margin-bottom: $space-xs; }
    - $space-s — assets/css/vars/_spacing.scss:80 — .space-s { margin-bottom: $space-s; }
    - $space-m — assets/css/vars/_spacing.scss:81 — .space-m { margin-bottom: $space-m; }
    - $space-l — assets/css/vars/_spacing.scss:82 — .space-l { margin-bottom: $space-l; }
    - $space-xl — assets/css/vars/_spacing.scss:83 — .space-xl { margin-bottom: $space-xl; }
    - $space-2xl — assets/css/vars/_spacing.scss:84 — .space-2xl { margin-bottom: $space-2xl; }
    - $space-3xl — assets/css/vars/_spacing.scss:85 — .space-3xl { margin-bottom: $space-3xl; }
    - $space-s — assets/css/vars/_spacing.scss:89 — margin-top: $space-s;
    - $space-s — assets/css/vars/_spacing.scss:93 — margin-top: $space-s;
    - $space-m — assets/css/vars/_spacing.scss:97 — margin-top: $space-m;
    - $space-l — assets/css/vars/_spacing.scss:101 — margin-top: $space-l;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $min-size — assets/css/vars/_spacing.scss:19 — @function fluid-space($min-size, $max-size) {
    - $max-size — assets/css/vars/_spacing.scss:19 — @function fluid-space($min-size, $max-size) {
    - $min-size — assets/css/vars/_spacing.scss:30 — @return clamp(#{$min-size}rem, #{$y-intercept}rem + #{$slope * 100}vw, #{$max-size}rem);
    - $y-intercept — assets/css/vars/_spacing.scss:30 — @return clamp(#{$min-size}rem, #{$y-intercept}rem + #{$slope * 100}vw, #{$max-size}rem);
    - $slope — assets/css/vars/_spacing.scss:30 — @return clamp(#{$min-size}rem, #{$y-intercept}rem + #{$slope * 100}vw, #{$max-size}rem);
    - $max-size — assets/css/vars/_spacing.scss:30 — @return clamp(#{$min-size}rem, #{$y-intercept}rem + #{$slope * 100}vw, #{$max-size}rem);

assets/css/vars/_typo.scss
  Type : global-vars
  Style scoped : non
  Déclarations :
    - $breakpoint-tablet — assets/css/vars/_typo.scss:15 — $breakpoint-tablet: 768px;
    - $light — assets/css/vars/_typo.scss:17 — $light: 300;
    - $normal — assets/css/vars/_typo.scss:18 — $normal: 500;
    - $bold — assets/css/vars/_typo.scss:19 — $bold: 700;
    - $black — assets/css/vars/_typo.scss:20 — $black: 900;
    - $type-ratios — assets/css/vars/_typo.scss:27 — $type-ratios: ( // "minorSecond": 1.067,
    - $type-base-size — assets/css/vars/_typo.scss:41 — $type-base-size: 1rem;
    - $type-size-ratio — assets/css/vars/_typo.scss:44 — $type-size-ratio: type-ratio("goldenRatio");
    - $type-levels — assets/css/vars/_typo.scss:48 — $type-levels: 4,
    - $type-styles — assets/css/vars/_typo.scss:55 — $type-styles: ();
    - $level-size — assets/css/vars/_typo.scss:56 — $level-size: $type-base-size;
    - $base-unitless — assets/css/vars/_typo.scss:57 — $base-unitless: math.div($type-base-size, ($type-base-size * 0 + 1));
    - $level-size — assets/css/vars/_typo.scss:60 — $level-size: $level-size * $type-size-ratio;
    - $level-size — assets/css/vars/_typo.scss:60 — $level-size: $level-size * $type-size-ratio;
    - $type-styles — assets/css/vars/_typo.scss:61 — $type-styles: map.merge($type-styles, (#{"h"}$level: $level-size));
    - $type-styles — assets/css/vars/_typo.scss:61 — $type-styles: map.merge($type-styles, (#{"h"}$level: $level-size));
    - $level-unitless — assets/css/vars/_typo.scss:83 — $level-unitless: math.div($level-size, ($level-size * 0 + 1));
    - $fluid-reduction — assets/css/vars/_typo.scss:88 — $fluid-reduction: 0.33; // valeur par défaut
    - $fluid-reduction — assets/css/vars/_typo.scss:90 — $fluid-reduction: 0.5;
    - $fluid-min — assets/css/vars/_typo.scss:92 — $fluid-min: $level-unitless - ($fluid-reduction * $level-unitless);
    - $fluid-min — assets/css/vars/_typo.scss:96 — $fluid-min: $base-unitless;
    - $fluid-scaler — assets/css/vars/_typo.scss:102 — $fluid-scaler: ($level-unitless - $fluid-min) + 4vw;
  Usages :
    - $type-base-size — assets/css/vars/_typo.scss:56 — $level-size: $type-base-size;
    - $type-base-size — assets/css/vars/_typo.scss:57 — $base-unitless: math.div($type-base-size, ($type-base-size * 0 + 1));
    - $type-base-size — assets/css/vars/_typo.scss:57 — $base-unitless: math.div($type-base-size, ($type-base-size * 0 + 1));
    - $type-size-ratio — assets/css/vars/_typo.scss:60 — $level-size: $level-size * $type-size-ratio;
    - $level — assets/css/vars/_typo.scss:61 — $type-styles: map.merge($type-styles, (#{"h"}$level: $level-size));
    - $level-size — assets/css/vars/_typo.scss:61 — $type-styles: map.merge($type-styles, (#{"h"}$level: $level-size));
    - $level — assets/css/vars/_typo.scss:65 — h#{$level},
    - $level — assets/css/vars/_typo.scss:66 — .h#{$level} {
    - $level-size — assets/css/vars/_typo.scss:68 — font-size: $level-size;
    - $level-size — assets/css/vars/_typo.scss:83 — $level-unitless: math.div($level-size, ($level-size * 0 + 1));
    - $level-size — assets/css/vars/_typo.scss:83 — $level-unitless: math.div($level-size, ($level-size * 0 + 1));
    - $level-unitless — assets/css/vars/_typo.scss:92 — $fluid-min: $level-unitless - ($fluid-reduction * $level-unitless);
    - $fluid-reduction — assets/css/vars/_typo.scss:92 — $fluid-min: $level-unitless - ($fluid-reduction * $level-unitless);
    - $level-unitless — assets/css/vars/_typo.scss:92 — $fluid-min: $level-unitless - ($fluid-reduction * $level-unitless);
    - $base-unitless — assets/css/vars/_typo.scss:96 — $fluid-min: $base-unitless;
    - $level-unitless — assets/css/vars/_typo.scss:102 — $fluid-scaler: ($level-unitless - $fluid-min) + 4vw;
    - $fluid-min — assets/css/vars/_typo.scss:102 — $fluid-scaler: ($level-unitless - $fluid-min) + 4vw;
    - $fluid-min — assets/css/vars/_typo.scss:104 — font-size: clamp(#{$fluid-min}rem,
    - $fluid-scaler — assets/css/vars/_typo.scss:105 — #{$fluid-scaler} + 1rem,
    - $level-size — assets/css/vars/_typo.scss:106 — #{$level-size});
  Variables dans commentaires :
    - $font-size — assets/css/vars/_typo.scss:1 — // $font-size: 100%;
    - $ratio — assets/css/vars/_typo.scss:2 — // $ratio: 1.333;
    - $h6 — assets/css/vars/_typo.scss:5 — // $h6: $font-size * $ratio;
    - $font-size — assets/css/vars/_typo.scss:5 — // $h6: $font-size * $ratio;
    - $ratio — assets/css/vars/_typo.scss:5 — // $h6: $font-size * $ratio;
    - $h5 — assets/css/vars/_typo.scss:6 — // $h5: $h6 * $ratio;
    - $h6 — assets/css/vars/_typo.scss:6 — // $h5: $h6 * $ratio;
    - $ratio — assets/css/vars/_typo.scss:6 — // $h5: $h6 * $ratio;
    - $h4 — assets/css/vars/_typo.scss:7 — // $h4: $h5 * $ratio;
    - $h5 — assets/css/vars/_typo.scss:7 — // $h4: $h5 * $ratio;
    - $ratio — assets/css/vars/_typo.scss:7 — // $h4: $h5 * $ratio;
    - $h3 — assets/css/vars/_typo.scss:8 — // $h3: $h4 * $ratio;
    - $h4 — assets/css/vars/_typo.scss:8 — // $h3: $h4 * $ratio;
    - $ratio — assets/css/vars/_typo.scss:8 — // $h3: $h4 * $ratio;
    - $h2 — assets/css/vars/_typo.scss:9 — // $h2: $h3 * $ratio;
    - $h3 — assets/css/vars/_typo.scss:9 — // $h2: $h3 * $ratio;
    - $ratio — assets/css/vars/_typo.scss:9 — // $h2: $h3 * $ratio;
    - $h1 — assets/css/vars/_typo.scss:10 — // $h1: $h2 * $ratio;
    - $h2 — assets/css/vars/_typo.scss:10 — // $h1: $h2 * $ratio;
    - $ratio — assets/css/vars/_typo.scss:10 — // $h1: $h2 * $ratio;
    - $level-size — assets/css/vars/_typo.scss:85 — // Set minimum size to a percentage less than $level-size
    - $type-ratios — assets/css/vars/_typo.scss:101 — // 4vw was selected by testing from $type-ratios map, YMMV 🙃
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $key — assets/css/vars/_typo.scss:36 — @function type-ratio($key) {
    - $type-ratios — assets/css/vars/_typo.scss:37 — @return map.get($type-ratios, $key);
    - $key — assets/css/vars/_typo.scss:37 — @return map.get($type-ratios, $key);
    - $level — assets/css/vars/_typo.scss:59 — @each $level in $type-levels {
    - $type-levels — assets/css/vars/_typo.scss:59 — @each $level in $type-levels {
    - $type-size-ratio — assets/css/vars/_typo.scss:79 — @if ($type-size-ratio > 1.2) {
    - $level-size — assets/css/vars/_typo.scss:89 — @if $level-size > 4 {
    - $fluid-min — assets/css/vars/_typo.scss:95 — @if $fluid-min <= $base-unitless {
    - $base-unitless — assets/css/vars/_typo.scss:95 — @if $fluid-min <= $base-unitless {
    - $key — assets/css/vars/_typo.scss:111 — @function type-style($key) {
    - $type-styles — assets/css/vars/_typo.scss:112 — @return map.get($type-styles, $key);
    - $key — assets/css/vars/_typo.scss:112 — @return map.get($type-styles, $key);

components/AppSearchInput.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — components/AppSearchInput.vue:83 — color: $vert;
  Variables dans commentaires :
    - $gris3 — components/AppSearchInput.vue:116 — //       color: $gris3;
    - $vert — components/AppSearchInput.vue:130 — //       border: 2px solid $vert;
    - $vert — components/AppSearchInput.vue:145 — //         background: $vert;
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/ArticleNavigation.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris1 — components/ArticleNavigation.vue:48 — color: $gris1;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/BaseButton.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — components/BaseButton.vue:43 — color: $vert;
    - $vert — components/BaseButton.vue:44 — background: linear-gradient($vert, $vert) right bottom / 0 0.1em no-repeat;
    - $vert — components/BaseButton.vue:44 — background: linear-gradient($vert, $vert) right bottom / 0 0.1em no-repeat;
    - $gris2 — components/BaseButton.vue:48 — color: $gris2;
    - $bleu2 — components/BaseButton.vue:52 — color: $bleu2;
    - $bleu2 — components/BaseButton.vue:53 — background: linear-gradient(color.adjust($bleu2, $lightness: 25%), color.adjust($bleu2, $lightness: 25%)) right
    - $bleu2 — components/BaseButton.vue:53 — background: linear-gradient(color.adjust($bleu2, $lightness: 25%), color.adjust($bleu2, $lightness: 25%)) right
    - $gris1 — components/BaseButton.vue:58 — color: $gris1;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/BoiteArticle.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris1 — components/BoiteArticle.vue:374 — color: $gris1;
    - $gris3 — components/BoiteArticle.vue:381 — color: $gris3;
    - $vert — components/BoiteArticle.vue:413 — color: color.adjust($vert, $lightness: -10%);
    - $gris3 — components/BoiteArticle.vue:432 — color: $gris3;
    - $gris2 — components/BoiteArticle.vue:439 — color: $gris2;
    - $gris3 — components/BoiteArticle.vue:530 — color: $gris3;
    - $gris2 — components/BoiteArticle.vue:538 — color: $gris2;
    - $gris2 — components/BoiteArticle.vue:555 — color: $gris2;
    - $vert — components/BoiteArticle.vue:584 — background: $vert;
    - $gris2 — components/BoiteArticle.vue:604 — color: $gris2;
    - $bleu2 — components/BoiteArticle.vue:658 — color: $bleu2 !important;
    - $bleu2 — components/BoiteArticle.vue:674 — color: $bleu2 !important;
    - $bleu2 — components/BoiteArticle.vue:689 — background: $bleu2;
    - $bleu2 — components/BoiteArticle.vue:708 — outline: 2px solid $bleu2;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — components/BoiteArticle.vue:300 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:321 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:342 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:351 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:458 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:463 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:480 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:488 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:503 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:576 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:621 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:714 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:719 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/BoiteArticle.vue:725 — @media (min-width: $breakpoint-tablet) {

components/Oeuf.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $jaune — components/Oeuf.vue:70 — stop-color: $jaune;
    - $vert — components/Oeuf.vue:73 — stop-color: $vert;
    - $bleu1 — components/Oeuf.vue:76 — stop-color: $bleu1;
    - $bleu2 — components/Oeuf.vue:79 — stop-color: $bleu2;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/TheFooter.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - $width — components/TheFooter.vue:84 — $width: 100vw;
    - $width — components/TheFooter.vue:86 — $width: 90vw;
    - $height — components/TheFooter.vue:88 — $height: 90vh;
    - $height — components/TheFooter.vue:90 — $height: calc($width / 1.618);
  Usages :
    - $width — components/TheFooter.vue:90 — $height: calc($width / 1.618);
    - $fondGris — components/TheFooter.vue:102 — background: $fondGris;
    - $gris4 — components/TheFooter.vue:199 — color: $gris4;
    - $vert — components/TheFooter.vue:208 — color: $vert;
    - $width — components/TheFooter.vue:215 — width: $width;
    - $height — components/TheFooter.vue:216 — height: $height;
    - $gris4 — components/TheFooter.vue:244 — color: $gris4;
  Variables dans commentaires :
    - $gris4 — components/TheFooter.vue:249 — //color: $gris4;
    - $gris4 — components/TheFooter.vue:401 — //   color: $gris4;
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — components/TheFooter.vue:85 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/TheFooter.vue:89 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/TheFooter.vue:143 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/TheFooter.vue:147 — @media (max-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/TheFooter.vue:160 — @media (max-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/TheFooter.vue:226 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/TheFooter.vue:327 — @media (min-width: $breakpoint-tablet) {

components/apps/AppBreadcrumb.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris3 — components/apps/AppBreadcrumb.vue:46 — color: $gris3;
    - $gris3 — components/apps/AppBreadcrumb.vue:65 — color: $gris3;
    - $vert — components/apps/AppBreadcrumb.vue:75 — outline: 2px solid $vert;
    - $gris1 — components/apps/AppBreadcrumb.vue:81 — color: $gris1;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/apps/AppCard.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris3 — components/apps/AppCard.vue:164 — color: $gris3;
    - $gris1 — components/apps/AppCard.vue:175 — color: $gris1;
    - $gris2 — components/apps/AppCard.vue:180 — color: $gris2;
    - $vert — components/apps/AppCard.vue:187 — color: $vert;
    - $vert — components/apps/AppCard.vue:203 — outline: 2px solid $vert;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/apps/AppFaqList.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris1 — components/apps/AppFaqList.vue:55 — color: $gris1;
    - $gris1 — components/apps/AppFaqList.vue:82 — color: $gris1;
    - $vert — components/apps/AppFaqList.vue:91 — color: $vert;
    - $gris2 — components/apps/AppFaqList.vue:107 — color: $gris2;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/apps/AppLegalTabs.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris3 — components/apps/AppLegalTabs.vue:148 — color: $gris3;
    - $vert — components/apps/AppLegalTabs.vue:156 — color: $vert;
    - $vert — components/apps/AppLegalTabs.vue:160 — outline: 2px solid $vert;
    - $gris1 — components/apps/AppLegalTabs.vue:170 — color: $gris1;
    - $gris2 — components/apps/AppLegalTabs.vue:177 — color: $gris2;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

components/apps/AppReleaseInterestForm.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — components/apps/AppReleaseInterestForm.vue:310 — color: $vert;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — components/apps/AppReleaseInterestForm.vue:191 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/apps/AppReleaseInterestForm.vue:202 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/apps/AppReleaseInterestForm.vue:234 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — components/apps/AppReleaseInterestForm.vue:339 — @media (min-width: $breakpoint-tablet) {

components/server/Oeuf.server.vue
  Type : component-scoped
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $jaune — components/server/Oeuf.server.vue:70 — stop-color: $jaune;
    - $vert — components/server/Oeuf.server.vue:73 — stop-color: $vert;
    - $bleu1 — components/server/Oeuf.server.vue:76 — stop-color: $bleu1;
    - $bleu2 — components/server/Oeuf.server.vue:79 — stop-color: $bleu2;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

error.vue
  Type : root-vue
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $bold — error.vue:115 — font-weight: $bold;
    - $gris2 — error.vue:116 — color: $gris2;
    - $bold — error.vue:123 — font-weight: $bold;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — error.vue:148 — @media (min-width: $breakpoint-tablet) {

layouts/default.vue
  Type : layout
  Style scoped : non
  Déclarations :
    - aucune
  Usages :
    - $vert — layouts/default.vue:444 — background: $vert;
    - $fondClair — layouts/default.vue:457 — color: $fondClair;
    - $light — layouts/default.vue:466 — font-weight: $light;
    - $vert — layouts/default.vue:513 — background: linear-gradient($vert, $vert) left bottom / 0 0.1em
    - $vert — layouts/default.vue:513 — background: linear-gradient($vert, $vert) left bottom / 0 0.1em
    - $bleu2 — layouts/default.vue:519 — color.adjust($bleu2, $lightness: 10%),
    - $bleu2 — layouts/default.vue:520 — color.adjust($bleu2, $lightness: 10%)
    - $bleu1 — layouts/default.vue:531 — background: linear-gradient($bleu1, $bleu1) left bottom / 0 0.1em
    - $bleu1 — layouts/default.vue:531 — background: linear-gradient($bleu1, $bleu1) left bottom / 0 0.1em
    - $jaune — layouts/default.vue:536 — background: linear-gradient($jaune, $jaune) left bottom / 0 0.1em
    - $jaune — layouts/default.vue:536 — background: linear-gradient($jaune, $jaune) left bottom / 0 0.1em
    - $fondClair — layouts/default.vue:560 — color: $fondClair;
    - $fondClair — layouts/default.vue:562 — fill: $fondClair;
    - $gris1 — layouts/default.vue:568 — color: $gris1;
    - $gris1 — layouts/default.vue:570 — fill: $gris1;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — layouts/default.vue:411 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — layouts/default.vue:461 — @media (min-width: $breakpoint-tablet) {

pages/404.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $bold — pages/404.vue:100 — font-weight: $bold;
    - $gris2 — pages/404.vue:101 — color: $gris2;
    - $bold — pages/404.vue:108 — font-weight: $bold;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/404.vue:133 — @media (min-width: $breakpoint-tablet) {

pages/apps/duo-spend.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — pages/apps/duo-spend.vue:421 — color: $vert;
    - $gris1 — pages/apps/duo-spend.vue:431 — color: $gris1;
    - $gris2 — pages/apps/duo-spend.vue:452 — color: $gris2;
    - $gris3 — pages/apps/duo-spend.vue:457 — color: $gris3;
    - $vert — pages/apps/duo-spend.vue:486 — background: $vert;
    - $gris2 — pages/apps/duo-spend.vue:493 — color: $gris2;
    - $gris1 — pages/apps/duo-spend.vue:499 — color: $gris1;
    - $vert — pages/apps/duo-spend.vue:506 — outline: 2px solid $vert;
    - $gris2 — pages/apps/duo-spend.vue:597 — color: $gris2;
    - $vert — pages/apps/duo-spend.vue:609 — color: $vert;
    - $gris3 — pages/apps/duo-spend.vue:643 — color: $gris3;
    - $gris1 — pages/apps/duo-spend.vue:652 — color: $gris1;
    - $gris2 — pages/apps/duo-spend.vue:671 — color: $gris2;
    - $vert — pages/apps/duo-spend.vue:747 — outline: 2px solid $vert;
    - $gris1 — pages/apps/duo-spend.vue:777 — color: $gris1;
    - $gris3 — pages/apps/duo-spend.vue:788 — color: $gris3;
    - $gris1 — pages/apps/duo-spend.vue:832 — color: $gris1;
    - $gris3 — pages/apps/duo-spend.vue:837 — color: $gris3;
    - $vert — pages/apps/duo-spend.vue:859 — background: $vert;
    - $vert — pages/apps/duo-spend.vue:913 — color: $vert;
    - $gris1 — pages/apps/duo-spend.vue:922 — color: $gris1;
    - $gris2 — pages/apps/duo-spend.vue:931 — color: $gris2;
    - $vert — pages/apps/duo-spend.vue:965 — background: $vert;
    - $vert — pages/apps/duo-spend.vue:975 — color: $vert;
    - $gris1 — pages/apps/duo-spend.vue:984 — color: $gris1;
    - $gris2 — pages/apps/duo-spend.vue:992 — color: $gris2;
    - $gris2 — pages/apps/duo-spend.vue:1007 — color: $gris2;
    - $vert — pages/apps/duo-spend.vue:1015 — color: $vert;
    - $gris1 — pages/apps/duo-spend.vue:1031 — color: $gris1;
    - $gris2 — pages/apps/duo-spend.vue:1036 — color: $gris2;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/apps/duo-spend.vue:388 — @media (min-width: $breakpoint-tablet) {

pages/apps/index.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — pages/apps/index.vue:142 — color: $vert;
    - $gris1 — pages/apps/index.vue:151 — color: $gris1;
    - $gris2 — pages/apps/index.vue:167 — color: $gris2;
    - $gris1 — pages/apps/index.vue:181 — color: $gris1;
    - $gris3 — pages/apps/index.vue:191 — color: $gris3;
    - $gris3 — pages/apps/index.vue:202 — color: $gris3;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/apps/index.vue:118 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/apps/index.vue:169 — @media (min-width: $breakpoint-tablet) {

pages/apps/meeting-mode.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — pages/apps/meeting-mode.vue:392 — color: $vert;
    - $gris1 — pages/apps/meeting-mode.vue:402 — color: $gris1;
    - $gris2 — pages/apps/meeting-mode.vue:423 — color: $gris2;
    - $gris3 — pages/apps/meeting-mode.vue:428 — color: $gris3;
    - $vert — pages/apps/meeting-mode.vue:457 — background: $vert;
    - $gris2 — pages/apps/meeting-mode.vue:464 — color: $gris2;
    - $gris1 — pages/apps/meeting-mode.vue:470 — color: $gris1;
    - $vert — pages/apps/meeting-mode.vue:477 — outline: 2px solid $vert;
    - $gris2 — pages/apps/meeting-mode.vue:568 — color: $gris2;
    - $gris1 — pages/apps/meeting-mode.vue:574 — color: $gris1;
    - $vert — pages/apps/meeting-mode.vue:586 — color: $vert;
    - $gris2 — pages/apps/meeting-mode.vue:613 — color: $gris2;
    - $vert — pages/apps/meeting-mode.vue:627 — background: $vert;
    - $vert — pages/apps/meeting-mode.vue:639 — background: $vert;
    - $gris2 — pages/apps/meeting-mode.vue:698 — color: $gris2;
    - $gris3 — pages/apps/meeting-mode.vue:702 — background: $gris3;
    - $vert — pages/apps/meeting-mode.vue:707 — color: $vert;
    - $vert — pages/apps/meeting-mode.vue:711 — background: $vert;
    - $gris2 — pages/apps/meeting-mode.vue:729 — color: $gris2;
    - $vert — pages/apps/meeting-mode.vue:764 — outline: 2px solid $vert;
    - $gris1 — pages/apps/meeting-mode.vue:794 — color: $gris1;
    - $gris3 — pages/apps/meeting-mode.vue:805 — color: $gris3;
    - $vert — pages/apps/meeting-mode.vue:843 — color: $vert;
    - $gris1 — pages/apps/meeting-mode.vue:852 — color: $gris1;
    - $gris2 — pages/apps/meeting-mode.vue:861 — color: $gris2;
    - $vert — pages/apps/meeting-mode.vue:903 — color: $vert;
    - $gris1 — pages/apps/meeting-mode.vue:923 — color: $gris1;
    - $gris3 — pages/apps/meeting-mode.vue:928 — color: $gris3;
    - $gris1 — pages/apps/meeting-mode.vue:954 — color: $gris1;
    - $gris2 — pages/apps/meeting-mode.vue:959 — color: $gris2;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/apps/meeting-mode.vue:359 — @media (min-width: $breakpoint-tablet) {

pages/apps/siturem.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — pages/apps/siturem.vue:429 — color: $vert;
    - $gris1 — pages/apps/siturem.vue:439 — color: $gris1;
    - $gris2 — pages/apps/siturem.vue:460 — color: $gris2;
    - $gris3 — pages/apps/siturem.vue:465 — color: $gris3;
    - $vert — pages/apps/siturem.vue:494 — background: $vert;
    - $gris2 — pages/apps/siturem.vue:501 — color: $gris2;
    - $gris1 — pages/apps/siturem.vue:507 — color: $gris1;
    - $vert — pages/apps/siturem.vue:514 — outline: 2px solid $vert;
    - $gris2 — pages/apps/siturem.vue:605 — color: $gris2;
    - $vert — pages/apps/siturem.vue:617 — color: $vert;
    - $gris3 — pages/apps/siturem.vue:651 — color: $gris3;
    - $gris1 — pages/apps/siturem.vue:660 — color: $gris1;
    - $gris2 — pages/apps/siturem.vue:679 — color: $gris2;
    - $vert — pages/apps/siturem.vue:755 — outline: 2px solid $vert;
    - $gris1 — pages/apps/siturem.vue:781 — color: $gris1;
    - $gris3 — pages/apps/siturem.vue:786 — color: $gris3;
    - $gris3 — pages/apps/siturem.vue:821 — color: $gris3;
    - $gris1 — pages/apps/siturem.vue:830 — color: $gris1;
    - $gris2 — pages/apps/siturem.vue:838 — color: $gris2;
    - $gris1 — pages/apps/siturem.vue:873 — color: $gris1;
    - $gris3 — pages/apps/siturem.vue:880 — color: $gris3;
    - $gris3 — pages/apps/siturem.vue:888 — border-right: 2px solid $gris3;
    - $gris3 — pages/apps/siturem.vue:889 — border-bottom: 2px solid $gris3;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/apps/siturem.vue:396 — @media (min-width: $breakpoint-tablet) {

pages/eco-conception/[slug].vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris3 — pages/eco-conception/[slug].vue:537 — border-top: 1px solid $gris3;
    - $gris4 — pages/eco-conception/[slug].vue:579 — border: 0.8px solid $gris4;
    - $vert — pages/eco-conception/[slug].vue:598 — color: $vert !important;
    - $gris4 — pages/eco-conception/[slug].vue:636 — border: 0.5px solid $gris4;
    - $space-s — pages/eco-conception/[slug].vue:674 — margin-bottom: $space-s;
    - $space-s — pages/eco-conception/[slug].vue:680 — margin-bottom: $space-s;
    - $space-2xs — pages/eco-conception/[slug].vue:684 — margin-bottom: $space-2xs;
    - $bold — pages/eco-conception/[slug].vue:690 — font-weight: $bold;
    - $gris3 — pages/eco-conception/[slug].vue:705 — background: linear-gradient($gris3, $gris3) right bottom / 100% 0.15em
    - $gris3 — pages/eco-conception/[slug].vue:705 — background: linear-gradient($gris3, $gris3) right bottom / 100% 0.15em
    - $vert — pages/eco-conception/[slug].vue:714 — background: linear-gradient($vert, $vert) right bottom / 100% 0.25em
    - $vert — pages/eco-conception/[slug].vue:714 — background: linear-gradient($vert, $vert) right bottom / 100% 0.25em
    - $space-xl — pages/eco-conception/[slug].vue:724 — margin-top: $space-xl; // Très grand espace AVANT (64px -> 104px)
    - $space-3xs — pages/eco-conception/[slug].vue:725 — margin-bottom: $space-3xs; // Minimal APRÈS (4px -> 6px)
    - $space-l — pages/eco-conception/[slug].vue:732 — margin-top: $space-l; // Grand espace AVANT (40px -> 64px)
    - $space-3xs — pages/eco-conception/[slug].vue:733 — margin-bottom: $space-3xs; // Minimal APRÈS (4px -> 6px)
    - $space-m — pages/eco-conception/[slug].vue:739 — margin-top: $space-m; // Espace moyen AVANT (24px -> 40px)
    - $space-3xs — pages/eco-conception/[slug].vue:740 — margin-bottom: $space-3xs; // Minimal APRÈS (4px -> 6px)
    - $gris6 — pages/eco-conception/[slug].vue:744 — background: $gris6;
    - $gris4 — pages/eco-conception/[slug].vue:745 — border-left: 10px solid $gris4;
    - $gris1 — pages/eco-conception/[slug].vue:746 — color: $gris1;
    - $gris6 — pages/eco-conception/[slug].vue:754 — background: $gris6;
    - $gris1 — pages/eco-conception/[slug].vue:755 — color: $gris1;
    - $vert — pages/eco-conception/[slug].vue:770 — color: $vert;
    - $gris1 — pages/eco-conception/[slug].vue:783 — color: $gris1;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/eco-conception/[slug].vue:530 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/[slug].vue:540 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/[slug].vue:630 — @media (min-width: $breakpoint-tablet) {

pages/eco-conception/index.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — pages/eco-conception/index.vue:1236 — color: color.adjust($vert, $lightness: 6%);
    - $vert — pages/eco-conception/index.vue:1310 — color.adjust($vert, $lightness: 14%),
    - $vert — pages/eco-conception/index.vue:1311 — $vert 72%
    - $gris1 — pages/eco-conception/index.vue:1313 — color: $gris1;
    - $vert — pages/eco-conception/index.vue:1478 — color.adjust($vert, $lightness: 8%),
    - $vert — pages/eco-conception/index.vue:1650 — color: color.adjust($vert, $lightness: 2%);
    - $vert — pages/eco-conception/index.vue:1761 — background: color.adjust($vert, $lightness: 4%);
    - $vert — pages/eco-conception/index.vue:1804 — color: color.adjust($vert, $lightness: 10%);
    - $vert — pages/eco-conception/index.vue:1993 — color: color.adjust($vert, $lightness: -6%);
    - $vert — pages/eco-conception/index.vue:2131 — color: color.adjust($vert, $lightness: -2%);
    - $vert — pages/eco-conception/index.vue:2240 — outline: 2px solid color.adjust($vert, $lightness: 8%);
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/eco-conception/index.vue:1196 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:1519 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:1526 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:1606 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:1710 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:1718 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:1810 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:2029 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/eco-conception/index.vue:2244 — @media (max-width: $breakpoint-tablet) {

pages/greenlight.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - aucun
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/greenlight.vue:745 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:760 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:767 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:773 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:781 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:978 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1111 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1128 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1151 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1194 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1201 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1207 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1213 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1303 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/greenlight.vue:1377 — @media (max-width: $breakpoint-tablet) {

pages/index.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — pages/index.vue:655 — color: $vert;
    - $vert — pages/index.vue:665 — background: $vert;
    - $vert — pages/index.vue:695 — color: color.adjust($vert, $lightness: 12%);
    - $vert — pages/index.vue:771 — color.adjust($vert, $lightness: 12%),
    - $vert — pages/index.vue:772 — $vert 70%
    - $gris1 — pages/index.vue:774 — color: $gris1;
    - $vert — pages/index.vue:832 — color: color.adjust($vert, $lightness: 18%);
    - $vert — pages/index.vue:931 — color: color.adjust($vert, $lightness: 14%);
    - $vert — pages/index.vue:959 — color: color.adjust($vert, $lightness: 12%);
    - $vert — pages/index.vue:1155 — color: color.adjust($vert, $lightness: 14%);
    - $vert — pages/index.vue:1169 — color: color.adjust($vert, $lightness: 12%);
    - $vert — pages/index.vue:1264 — outline: 2px solid color.adjust($vert, $lightness: 16%);
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/index.vue:621 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:803 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:859 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:866 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:876 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:902 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:995 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:1016 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:1117 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:1143 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:1220 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/index.vue:1268 — @media (max-width: $breakpoint-tablet) {

pages/portfolio.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $gris1 — pages/portfolio.vue:391 — color: $gris1;
    - $vert — pages/portfolio.vue:412 — background: $vert;
    - $vert — pages/portfolio.vue:414 — border-color: $vert;
    - $gris3 — pages/portfolio.vue:450 — color: $gris3;
    - $gris1 — pages/portfolio.vue:463 — color: $gris1;
    - $gris3 — pages/portfolio.vue:473 — color: $gris3;
    - $vert — pages/portfolio.vue:542 — text-decoration-color: $vert;
    - $gris2 — pages/portfolio.vue:591 — color: $gris2;
    - $gris1 — pages/portfolio.vue:603 — border-color: $gris1;
    - $gris2 — pages/portfolio.vue:627 — color: $gris2;
    - $vert — pages/portfolio.vue:750 — background: $vert;
    - $vert — pages/portfolio.vue:782 — background: $vert;
    - $vert — pages/portfolio.vue:788 — background: $vert;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - aucune

pages/services.vue
  Type : page
  Style scoped : oui
  Déclarations :
    - aucune
  Usages :
    - $vert — pages/services.vue:608 — border-left: 0.25rem solid $vert;
    - $vert — pages/services.vue:783 — background: $vert;
    - $vert — pages/services.vue:789 — background: color.adjust($vert, $lightness: 6%);
    - $vert — pages/services.vue:880 — color: color.adjust($vert, $lightness: -16%);
    - $vert — pages/services.vue:920 — background: $vert;
  Variables dans commentaires :
    - aucune
  Variables dans @use/@include/@mixin/@function/@if/@each/@for :
    - $breakpoint-tablet — pages/services.vue:563 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/services.vue:814 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/services.vue:943 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/services.vue:993 — @media (min-width: $breakpoint-tablet) {
    - $breakpoint-tablet — pages/services.vue:1131 — @media (max-width: $breakpoint-tablet) {
