# Netlify Configuration for Nuxt 3
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  # Use supported build image (Noble LTS until 2029)
  image = "ubuntu-24.04"
  # Build command for Nuxt 3 static generation (force npm to avoid yarn/npm mix)
  command = "npm run generate"

  # Output directory (Nuxt 3 default)
  publish = ".output/public"

  # Environment variables
  environment = { NODE_VERSION = "18", NUXT_PUBLIC_SITE_URL = "https://beabot.netlify.app", NETLIFY_USE_YARN = "false", NITRO_PRESET = "static", PYTHON_VERSION = "3.11.8" }

# Build optimizations
[build.processing]
  # Skip Netlify's processing (Nuxt already optimizes)
  skip_processing = false

[build.processing.css]
  bundle = true
  minify = true

[build.processing.js]
  bundle = true
  minify = true

[build.processing.html]
  pretty_urls = true

[build.processing.images]
  compress = true

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    # Security headers
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache Nuxt build assets (CSS, JS) for 1 year
[[headers]]
  for = "/_nuxt/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache optimized images for 1 year
[[headers]]
  for = "/_ipx/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache public images for 1 year
[[headers]]
  for = "/img/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.html"
  [headers.values]
    # Don't cache HTML files
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/sw.js"
  [headers.values]
    # Don't cache service worker
    Cache-Control = "public, max-age=0, must-revalidate"

# CV PDF - Non-indexable
[[headers]]
  for = "/cv.pdf"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
    Cache-Control = "private, max-age=3600"

[[headers]]
  for = "/CV-*.pdf"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
    Cache-Control = "private, max-age=3600"

# Redirects and rewrites
[[redirects]]
  from = "https://beabot.netlify.com/*"
  to = "https://beabot.netlify.app/:splat"
  status = 301
  force = true

# SPA fallback for client-side routing (if needed)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200

# Netlify Forms configuration (for contact form)
# No additional config needed - handled by data-netlify="true" in HTML

# Plugin configuration
[[plugins]]
  package = "@netlify/plugin-lighthouse"

  [plugins.inputs]
    # Run Lighthouse audits on deploy
    output_path = "reports/lighthouse.html"

[[plugins]]
  package = "netlify-plugin-submit-sitemap"

  [plugins.inputs]
    # Submit sitemap to search engines
    baseUrl = "https://beabot.netlify.app"
    sitemapPath = "/sitemap.xml"
    providers = [
      "google",
      "bing"
    ]

# Environment variables (example - set in Netlify UI for security)
# [build.environment]
#   NUXT_PUBLIC_SITE_URL = "https://beabot.netlify.app"

# Context-specific builds
[context.production]
  command = "npm run generate"

[context.deploy-preview]
  command = "npm run generate"

[context.branch-deploy]
  command = "npm run generate"

# Dev environment (Netlify Dev)
[dev]
  command = "npm run dev"
  port = 3000
  targetPort = 3000
  publish = ".output/public"
